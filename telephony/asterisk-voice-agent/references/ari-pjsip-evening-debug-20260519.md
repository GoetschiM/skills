# Evening Debug Session — 2026-05-19 17:35–19:40

## Summary

Tried 6 callbot versions across ARI and Dialplan approaches to get bidirectional voice working on PJSIP/Salt-Trunk. Core issue: custom sound files (even GSM) produce no audio.

## v1–v3 (early session, not in this evening window)
- v1: Basic ARI callbot with fixed sleeps
- v2: ARI callbot with TTS integration
- v3: Fixed sleep-based, works for single playback

## v4 — Event-driven ARI callbot
- ARI WebSocket + event-driven: waits for PlaybackFinished/RecordingFinished
- **Outcome:** Welcome plays (PlaybackFinished fires) but Michel hears NO audio
- Log shows all events firing correctly

## v5 — Debug ARI (standard sound first)
- Plays `tt-weasels` (standard Asterisk built-in GSM) FIRST, then custom `hermes_welcome`
- **Outcome:** `tt-weasels` heard by Michel ("fast kinesisch"), but `hermes_welcome` produces NO audio. RTP/Salt times out after 30s.

## Bridge workaround (separate test)
- ARI mixing bridge: `POST /bridges/{bridgeId}/play` instead of channel play
- **Outcome:** SAME problem — first sound plays, second sound dead

## v6 — ARI Dialplan redirect
- `POST /channels/{channelId}/redirect {"endpoint": "Local/exten@context"}`
- **Outcome:** HTTP 422 — `"Endpoint technology 'Local' does not match channel technology 'PJSIP'"`

## Dialplan-based outbound call (`hermes-out` + `hermes-conversation`)
- Uses `channel originate Local/079...@hermes-out extension s@hermes-conversation`
- Same as Apollo's proven pattern
- **Outcome:** Calls go through, Playback executes, Record() saves 160KB WAV files
- **BUT:** Custom `hermes_welcome.gsm` still produces NO audio
- **KNOWN WORKING:** `apollo_goodbye.gsm`, `apollo_test.gsm`, `apollo_hello.gsm`, `tt-weasels.gsm`

## Key Discovery: Nova's API
- Service: `/opt/hermes167/data/scripts/nova-call-api.py` (FastAPI)
- Port: 5001
- Endpoints: `/health`, `/trigger-call`, `/history`
- Docs: `http://10.0.60.167:5001/docs`
- Also: `/opt/hermes167/data/sd-api/app.py` on port 3020 (Stable Diffusion)

## Open Questions
1. **Why do ffmpeg-generated GSM files fail but Apollo's existing GSM files work?** Both are raw GSM at 8000Hz mono. Hex dumps show identical frame header bytes (`d8 20 a2 e1 5a 50 00 49...`).
2. **Could the issue be in edge-tts output format?** edge-tts outputs 24kHz MP3. Maybe the ffmpeg conversion pipeline introduces artifacts that Asterisk/PJSIP can't handle.
3. **What toolchain did Apollo use?** Check `/opt/hermes167/` for Apollo's scripts. Maybe `sox` or a different ffmpeg build.
4. **Does the issue happen with ALL custom files or only edge-tts output?** Test with a custom WAV generated by other means (e.g. recording a real voice).

## Files Mentioned
- `/usr/local/bin/ari_callbot_v4.py` — Event-driven ARI
- `/usr/local/bin/ari_callbot_v5.py` — Debug with tt-weasels
- `/usr/local/bin/ari_callbot_v6.py` — Dialplan redirect attempt
- `/usr/local/bin/ari_test_simple.py` — Sequential play test
- `/usr/local/bin/ari_bridge_test.py` — Bridge play test
- `/var/lib/asterisk/sounds/hermes_welcome.*` — All formats (wav, alaw, ulaw, gsm)
- `/var/lib/asterisk/sounds/apollo_goodbye.gsm` — Known working (15378 bytes, 9.32s)
- `/tmp/hermes_input_*.wav` — Dialplan Record() output (160KB each, 10s)
- `/tmp/ari_callbot_v*.log` — Session logs
- `/tmp/ari_test.log` — Sequential test log
- `/tmp/ari_bridge_test.log` — Bridge test log
