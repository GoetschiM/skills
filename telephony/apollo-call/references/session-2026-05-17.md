# Session Notes — 17.05.2026

## Asterisk Architecture Findings

### CDR Diagnosis
- Calls via `Local/0796459743@apollo-out` show **NO ANSWER** from Salt trunk on first attempt (0s duration in CDR)
- 3 parallel originates in quick succession → first fails, second/third connects
- CDR format: `/var/log/asterisk/cdr-csv/Master.csv` (pipe-delimited)

### ARI Limitations
- `channels.create` via ARI without WebSocket Stasis app → channel routed to `from-salt-inbound` context then immediately torn down
- ARI user: `henryari` / `DEIN_STARKES_AMI_PASSWORT`
- ARI alone is NOT sufficient for outbound calls — only Nova with her Stasis app can do local calls via ARI

### PJSIP Endpoints
| Endpoint | Ext | Status | Purpose |
|----------|-----|--------|---------|
| PJSIP/apollo | 100 | offline | Test/Dev, auth=100/100 |
| PJSIP/101 | 101 | offline | Michel extension, auth=101/101 |
| PJSIP/salt-trunk | - | online | Salt.ch SIP trunk to mobile |

### Salt Trunk
- Registration: `sip:+4131****7977@sipvoice.salt.ch` (registered, contact Avail)
- Client URI format with `+41` prefix
- Dial pattern: `PJSIP/salt-trunk/sip:+41${EXTEN:1}@sipvoice.salt.ch`
- DNS: `sipvoice.salt.ch` → `213.55.128.83`, `213.55.128.211`
- First originate attempt reliably NO ANSWER — retry pattern essential

### TEAM-8 (Cronjob Registry)
- Every agent must log their cronjobs as a comment in TEAM-8
- Current Hermes cron: Gmail-PDF-Tages-Scan (ID 7c7cd57438f0)
- Schedule: daily 08:00 UTC
- Update TEAM-8 whenever a cronjob is created, modified, or deleted
