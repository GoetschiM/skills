# OpenClaw Office Analysis

**⚠️ THIS REFERENCE IS DEPRECATED — ACCURATE INFO MOVED**

The detailed, up-to-date OpenClaw Office analysis now lives in the **`threejs-agent-visualization`** skill:

- **`skill_view(name='threejs-agent-visualization')`** — SKILL.md (how to build 3D agent worlds with R3F)
- **`skill_view(name='threejs-agent-visualization', file_path='references/openclaw-office-reference.md')`** — Full reference implementation analysis (correct AgentAvatar.tsx, types, server.ts, screenshots)

The `goetschi-labs-site-mcp` skill covers website *deployment only* — for 3D agent visualization implementation, use the threejs-agent-visualization skill.

## Cross-reference

The Team Hero (CSS floating avatars) was built 13.06.2026. The user then requested a full 3D Sims-style agent world (OpenClaw style) — that's tracked in the `threejs-agent-visualization` skill as the next evolution.
